def counter(s,1):
  string = str(s)
  letter = str(1)
  count = 0
  for thing in string:
    if thing == letter:
      count = count+1
      print(count)
      counter('deepak','a')
      counter('banana','b')
